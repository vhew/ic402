node_modules/
dist/
out/
.icp/cache/
.icp/tools/
.mops/
.pnpm-store/
*.wasm

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.*
!.env.example

# Identity / keys
*.pem
*.key
.local/

# Patch backup files (created by scripts/patch-local.sh during local deploy).
# These hold the pristine MAINNET source while it is temporarily testnet-patched.
# A failed restore can leave testnet-patched content behind — never commit these.
*.local-bak
example/main.mo.local-bak

# Deploy (local-only tooling, not part of the library)
deploy/

# Foundry build artifacts
contracts/out/
contracts/cache/

# Rust (zk-verifier) build dir — the prebuilt deploy artifact lives in
# example/zk-verifier/prebuilt/zk_verifier.wasm.gz (committed); target/ is build cache.
example/zk-verifier/target/

# Claude Code
.claude/

# npm auth — a PER-REPO literal token lives in a LOCAL, gitignored `.npmrc` (never committed).
# pnpm won't expand env vars in a project .npmrc, so the token is literal; gitignore keeps it safe.
# `.npmrc.example` is the tracked template — create yours with `pnpm config set … --location project`.
.npmrc
!.npmrc.example
